import 'package:flutter/material.dart';

class homeApoderado extends StatelessWidget{
  const homeApoderado({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}